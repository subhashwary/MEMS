if (labels.length > 300) {

    labels.shift();

    pressureData.shift();
    dmmData.shift();

    psuVoltageData.shift();
    psuCurrentData.shift();

    essData.shift();
    modeData.shift();

    cycleData.shift();
}
